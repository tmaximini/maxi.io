import React from 'react'
import styled from 'styled-components'

const P = styled.p`
  margin: 0 auto;
  margin-bottom: 20px;
  max-width: 40em;
  text-align: center;
  color: rgba(0, 0, 0, 0.7);
  line-height: 1.65;
  font-size: 0.9em;
`

export default P
