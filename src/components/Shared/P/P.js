import React from 'react'
import styled from 'styled-components'

const P = styled.p`
  margin-bottom: 20px;
  max-width: 40em;
  text-align: center;
  color: rgba(0, 0, 0, 0.7);
  line-height: 1.65;
  margin: 0 auto;
`

export default P
